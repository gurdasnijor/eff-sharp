namespace Effect

/// `Runtime` — planned (slice 4).
/// Port reference: repos/effect-smol/packages/effect/src/Runtime.ts
[<RequireQualifiedAccess>]
module Runtime =
    /// Placeholder until slice 4 lands; replace with the ported API.
    let internal todo = "slice 4"

namespace Effect

/// `Exit` — planned (slice 2).
/// Port reference: repos/effect-smol/packages/effect/src/Exit.ts
[<RequireQualifiedAccess>]
module Exit =
    /// Placeholder until slice 2 lands; replace with the ported API.
    let internal todo = "slice 2"

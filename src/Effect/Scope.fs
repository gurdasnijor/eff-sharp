namespace Effect

/// `Scope` — planned (slice 3).
/// Port reference: repos/effect-smol/packages/effect/src/Scope.ts
[<RequireQualifiedAccess>]
module Scope =
    /// Placeholder until slice 3 lands; replace with the ported API.
    let internal todo = "slice 3"

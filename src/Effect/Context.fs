namespace Effect

/// `Context` — planned (slice 5).
/// Port reference: repos/effect-smol/packages/effect/src/Context.ts
[<RequireQualifiedAccess>]
module Context =
    /// Placeholder until slice 5 lands; replace with the ported API.
    let internal todo = "slice 5"

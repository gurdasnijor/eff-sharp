namespace Effect

/// `Stream` — planned (slice 6).
/// Port reference: repos/effect-smol/packages/effect/src/Stream.ts
[<RequireQualifiedAccess>]
module Stream =
    /// Placeholder until slice 6 lands; replace with the ported API.
    let internal todo = "slice 6"

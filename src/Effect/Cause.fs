namespace Effect

/// `Cause` — planned (slice 2).
/// Port reference: repos/effect-smol/packages/effect/src/Cause.ts
[<RequireQualifiedAccess>]
module Cause =
    /// Placeholder until slice 2 lands; replace with the ported API.
    let internal todo = "slice 2"

module TxSubscriptionRefSpec

// TODO(tx-vitest): Port the 4 old TxSubscriptionRef xUnit facts once the
// Fable/Vitest harness can compile STM sources. `test/Harness.fsproj` currently
// removes `$(EffectSrcDir)Tx*.fs`, so direct references to `TxSubscriptionRef`
// fail at compile time.

module TxSemaphoreSpec

// TODO(tx-vitest): Port the 19 old TxSemaphore xUnit facts once the
// Fable/Vitest harness can compile STM sources. `test/Harness.fsproj` currently
// removes `$(EffectSrcDir)Tx*.fs`, so direct references to `TxSemaphore` fail at
// compile time.

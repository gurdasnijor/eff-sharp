module TxPriorityQueueSpec

// TODO(tx-vitest): Port the 14 old TxPriorityQueue xUnit facts once the
// Fable/Vitest harness can compile STM sources. `test/Harness.fsproj` currently
// removes `$(EffectSrcDir)Tx*.fs`, so direct references to `TxPriorityQueue`
// fail at compile time.

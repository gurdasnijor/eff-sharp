module TxPubSubSpec

// TODO(tx-vitest): Port the 8 old TxPubSub xUnit facts once the Fable/Vitest
// harness can compile STM sources. `test/Harness.fsproj` currently removes
// `$(EffectSrcDir)Tx*.fs`, so direct references to `TxPubSub` fail at compile time.

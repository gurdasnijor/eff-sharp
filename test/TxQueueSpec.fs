module TxQueueSpec

// TODO(tx-vitest): Port the 11 old TxQueue xUnit facts once the Fable/Vitest
// harness can compile STM sources. `test/Harness.fsproj` currently removes
// `$(EffectSrcDir)Tx*.fs`, so direct references to `TxQueue` fail at compile time.

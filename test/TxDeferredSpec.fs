module TxDeferredSpec

// TODO(tx-vitest): Port the 4 old TxDeferred xUnit facts once the Fable/Vitest
// harness can compile STM sources. `test/Harness.fsproj` currently removes
// `$(EffectSrcDir)Tx*.fs`, so direct references to `TxDeferred` fail at compile time.

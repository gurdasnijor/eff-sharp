module TxHashSetSpec

// TODO(tx-vitest): Port the 4 old TxHashSet xUnit facts once the Fable/Vitest
// harness can compile STM sources. `test/Harness.fsproj` currently removes
// `$(EffectSrcDir)Tx*.fs`, so direct references to `TxHashSet` fail at compile time.

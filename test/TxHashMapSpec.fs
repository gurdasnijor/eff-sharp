module TxHashMapSpec

// TODO(tx-vitest): Port the 4 old TxHashMap xUnit facts once the Fable/Vitest
// harness can compile STM sources. `test/Harness.fsproj` currently removes
// `$(EffectSrcDir)Tx*.fs`, so direct references to `TxHashMap` fail at compile time.
